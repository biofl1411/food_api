# 🍽️ 식품 영양정보 검색 플랫폼

공공데이터포털 API를 활용한 식품 영양성분 검색 웹 서비스

## 주요 기능

- 🔍 **식품 검색**: 식품명으로 영양정보 검색
- 📊 **영양성분 조회**: 열량, 탄수화물, 단백질, 지방, 당류, 나트륨 정보 확인
- 📱 **반응형 UI**: 모바일/데스크톱 모두 지원
- 📄 **페이지네이션**: 대량의 검색 결과 페이지 단위 탐색

## 기술 스택

- **Backend**: Python FastAPI
- **Frontend**: HTML5, CSS3, Vanilla JavaScript
- **API**: 공공데이터포털 식품영양성분 데이터베이스 API

## 프로젝트 구조

```
food_api/
├── app/
│   ├── main.py                 # FastAPI 메인 서버
│   ├── services/
│   │   └── food_api.py         # 공공데이터 API 서비스
│   └── static/
│       ├── index.html          # 메인 페이지
│       ├── css/
│       │   └── style.css       # 스타일시트
│       └── js/
│           └── app.js          # 프론트엔드 스크립트
├── requirements.txt            # Python 의존성
├── .env.example               # 환경변수 예시
└── README.md
```

## 설치 및 실행

### 1. 의존성 설치

```bash
pip install -r requirements.txt
```

### 2. 환경변수 설정

```bash
cp .env.example .env
# .env 파일에서 PUBLIC_DATA_API_KEY 설정
```

> API 키는 [공공데이터포털](https://www.data.go.kr)에서 발급받을 수 있습니다.
> API 키 없이도 샘플 데이터로 데모 실행이 가능합니다.

### 3. 서버 실행

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 1411
```

### 4. 접속

브라우저에서 http://localhost:1411 접속

## API 엔드포인트

| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/` | 메인 검색 페이지 |
| GET | `/api/search?q={keyword}&page={n}&per_page={n}` | 식품 검색 API |
| GET | `/api/health` | 헬스 체크 |

## 스크린샷

### 메인 검색 화면
- 깔끔한 검색 인터페이스
- 식품명 입력 후 검색

### 검색 결과
- 식품별 카드 형태로 영양정보 표시
- 열량, 탄수화물, 단백질, 지방, 당류, 나트륨 정보

## 라이선스

MIT License

## 데이터 출처

- [공공데이터포털](https://www.data.go.kr)
- 식품의약품안전처 식품영양성분 데이터베이스
